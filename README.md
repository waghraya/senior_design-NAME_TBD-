# senior_design-NAME_TBD-

This simulator is based on the orekit_jpype library which is a python wrapper of the orekit java library.

To use it, a JDK (11+) must be added as an environment variable including a JAVA_HOME variable.
